.btn
  display: inline-block;
  padding: 0.4em 1em;
  border: 1px solid #444;
  border-radius: 3px;
  text-decoration: none;
  color: #444;
  position: relative;
  background:rgba(white, 0);

  &:disabled {
    opacity:0.4;
  }